Todo for next demo:
Add preview images for major builds
Scroll bar for the build selection
Make the build selection look better
Add actual font instead of using system font
Menu for downloading builds from ~jmaloney
Make the program less stingy with paths

Long-term todo (for now):
Add a favourites feature
Add preview images for all builds
Add support for 2.0 and 3.0
Make the right side of the window less empty
Add folders
Add some sort of feature to encourage modding
Add a feature specifically for any youtubers looking at old builds
Make the about page better